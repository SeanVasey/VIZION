# =====================================================================
# VIZ(IO)N — environment template.  Copy to `.env.local` and fill in.
# NEVER commit real values. `.env*.local` is gitignored.
# =====================================================================

# --- Supabase (P2+) -------------------------------------------------
# Public (safe to expose to the browser):
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — bypasses RLS, NEVER expose to the client:
SUPABASE_SERVICE_ROLE_KEY=

# --- Model providers (P3+) — server-side only -----------------------
# These keys must never reach the client. They are read exclusively
# inside Next route handlers (the provider-adapter proxy).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
# xAI key for the Grok 4.5 target — NEW: must also be added to the Vercel
# project env; until it is set, Grok returns 503 "not configured".
XAI_API_KEY=
# Mistral key for the Mistral Large 3 target — must also be added to the
# Vercel project env; until it is set, Mistral returns 503 "not configured".
MISTRAL_API_KEY=

# Model strings per target (overridable — keeps the adapter version-agnostic, D9).
# Defaults: claude-opus-4-8 / gpt-5.6-sol / claude-fable-5 / gemini-3.5-thinking
# / mistral-large-latest / grok-4.5.
MODEL_OPUS=
MODEL_GPT=
MODEL_FABLE=
MODEL_GEMINI=
MODEL_MISTRAL=
MODEL_GROK=
# Optional USD-per-1M-token pricing overrides used for the cost cap.
PRICE_OPUS_IN=
PRICE_OPUS_OUT=
PRICE_GPT_IN=
PRICE_GPT_OUT=
PRICE_FABLE_IN=
PRICE_FABLE_OUT=
PRICE_GEMINI_IN=
PRICE_GEMINI_OUT=
PRICE_MISTRAL_IN=
PRICE_MISTRAL_OUT=
PRICE_GROK_IN=
PRICE_GROK_OUT=

# --- Per-user cost caps & rate limits (P3+) -------------------------
# Enforced server-side on every model route.
COST_CAP_USD_PER_DAY=2.00
RATE_LIMIT_PER_MIN=20

# --- Media (P5) -----------------------------------------------------
# Extraction pipeline: "proxy" (vision via the model proxy, default) or
# "ondevice" (fast, private, limited). Proxy needs ANTHROPIC_API_KEY.
NEXT_PUBLIC_MEDIA_EXTRACTION=proxy

# --- App ------------------------------------------------------------
# Canonical origin used for absolute URLs (OAuth redirects, share links).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Skills

Project-specific skills for VIZ(IO)N live here. None are defined yet.

Candidates as the build progresses:

- **provider-formatter** — scaffold a new per-model formatter (Opus/GPT/Gemini) with the
  adapter contract and a golden-file test (P3).
- **rls-migration** — generate a Supabase migration with its RLS policy in the same file
  so no table ever ships without one (P2/P4).

See https://docs.claude.com/en/docs/claude-code/skills for the skill format.

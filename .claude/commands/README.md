# Commands

Project slash-commands for VIZ(IO)N live here as Markdown files (`<name>.md`). None are
defined yet.

Candidate: **`/verify`** — run the full gate
(`lint → typecheck → test → test:e2e → build`) and report the first failure.

See https://docs.claude.com/en/docs/claude-code/slash-commands for the format.
